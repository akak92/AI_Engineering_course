# Documento de ejemplo (reemplazar)

Este es un archivo de ejemplo para que `ingest.py`/`rag_chain.py` tengan
algo que indexar apenas copies este template.

# TODO: borrá este archivo y agregá tus propios documentos `.md`/`.txt`
# acá (manuales, apuntes, normativas, documentación técnica, etc.).

## Sección de ejemplo

Reemplazá este contenido por texto real de tu dominio. El pipeline de
ingesta (`ingest.py`) va a fragmentar automáticamente cualquier archivo
`.md`/`.txt` que encuentre en esta carpeta.
